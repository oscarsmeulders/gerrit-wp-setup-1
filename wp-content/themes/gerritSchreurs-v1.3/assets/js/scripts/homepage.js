jQuery(document).ready(function($){



	$('.one').cycle({

	});
	$('.two').cycle({

	});
	$('.three').cycle({

	});
	$('.four').cycle({

	});
	$('.five').cycle({

	});
	$('.six').cycle({

	});

	// http://jquery.malsup.com/cycle/timeout.html
});